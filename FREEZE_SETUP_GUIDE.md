# Руководство по запуску геймификации FREEZE

## 🚀 Быстрый старт

### 1. Настройка переменных окружения

Добавьте следующие переменные в ваш `.env` файл:

```env
NUXT_GAMIFICATION_FREEZE_ENABLED=true
NUXT_GAMIFICATION_FREEZE_CHANCE=0.1
NUXT_GAMIFICATION_FREEZE_DURATION=300000
NUXT_GAMIFICATION_FREEZE_COMMISSION=1
```

### Описание переменных:

- **NUXT_GAMIFICATION_FREEZE_ENABLED** - включение/выключение акции (true/false)
- **NUXT_GAMIFICATION_FREEZE_CHANCE** - шанс активации при входе (0.1 = 10%, 1 = 100%)
- **NUXT_GAMIFICATION_FREEZE_DURATION** - длительность акции в миллисекундах (300000 = 5 минут)
- **NUXT_GAMIFICATION_FREEZE_COMMISSION** - фиксированная комиссия в процентах (1 = 1%)

### 2. Установка зависимостей

Если вы еще не установили зависимости проекта:

```bash
npm install
```

### 3. Запуск dev сервера

```bash
npm run dev
```

Приложение будет доступно на `http://localhost:8081`

## 🎯 Как протестировать

### Тестирование активации

1. **Гарантированная активация (для тестов)**
   
   Установите в `.env`:
   ```env
   NUXT_GAMIFICATION_FREEZE_CHANCE=1
   ```
   
   Теперь акция будет активироваться при каждом заходе на страницу.

2. **Проверка модалки**
   
   - При активации должна появиться модалка с информацией об акции
   - Модалка содержит анимацию снежинок
   - Показывается таймер обратного отсчета
   - Кнопка "Начать обмен" закрывает модалку

3. **Проверка плашки (badge)**
   
   - В правом верхнем углу появляется плашка с "1%"
   - Плашка содержит анимацию ветра на фоне
   - Показывается таймер обратного отсчета
   - Плашка автоматически исчезает после истечения времени

4. **Проверка скрытия промокода**
   
   - Перейдите на страницу обмена
   - При активной акции FREEZE поле "Промокод" должно быть скрыто
   - После истечения акции поле промокода снова появляется

5. **Проверка комиссии**
   
   - При активной акции комиссия должна быть зафиксирована на уровне 1%
   - Независимо от суммы обмена (< 10000 или >= 10000 RUB)
   - Проверьте расчеты для разных пар валют

### Тестирование персистентности

1. **Обновление страницы**
   
   - Активируйте акцию
   - Обновите страницу (F5)
   - Акция должна продолжать работать с оставшимся временем

2. **Закрытие и открытие вкладки**
   
   - Активируйте акцию
   - Закройте вкладку
   - Откройте страницу снова
   - Если время не истекло - акция должна восстановиться

3. **Cooldown проверка**
   
   - После завершения акции подождите
   - Попробуйте активировать снова в течение часа
   - Акция не должна активироваться (cooldown 1 час)
   - Чтобы сбросить cooldown, очистите localStorage:
   ```javascript
   localStorage.removeItem('mfz_freeze_history')
   ```

## 🔧 Настройка для production

### Рекомендуемые значения для боевого окружения:

```env
# Production settings
NUXT_GAMIFICATION_FREEZE_ENABLED=true
NUXT_GAMIFICATION_FREEZE_CHANCE=0.05    # 5% шанс
NUXT_GAMIFICATION_FREEZE_DURATION=300000  # 5 минут
NUXT_GAMIFICATION_FREEZE_COMMISSION=1     # 1%
```

### Мониторинг и аналитика

Акция отправляет следующие события для аналитики:

1. **freeze_activated** - акция активирована
2. **freeze_modal_shown** - модалка показана пользователю
3. **freeze_modal_closed** - пользователь закрыл модалку
4. **freeze_exchange_completed** - обмен завершен с активной акцией
5. **freeze_expired** - время акции истекло

События отправляются в:
- Google Analytics (gtag)
- Яндекс.Метрика (ym)
- Console.log (для отладки)

## 🐛 Отладка

### Проверка состояния в консоли браузера:

```javascript
// Проверить текущее состояние акции
localStorage.getItem('mfz_freeze_state')

// Проверить историю активаций
localStorage.getItem('mfz_freeze_history')

// Очистить все данные акции
localStorage.removeItem('mfz_freeze_state')
localStorage.removeItem('mfz_freeze_history')

// Форсировать активацию (добавить в composable для тестов)
// useFreezePromotion().activateFreeze()
```

### Распространенные проблемы:

1. **Акция не активируется**
   - Проверьте `NUXT_GAMIFICATION_FREEZE_ENABLED=true`
   - Проверьте cooldown (очистите localStorage)
   - Увеличьте шанс до 1 для тестирования

2. **Модалка не появляется**
   - Проверьте консоль на ошибки
   - Убедитесь что `FreezeWrapper` добавлен в `app.vue`

3. **Комиссия не применяется**
   - Проверьте интеграцию в `useFactor.ts`
   - Убедитесь что `isFreezed.value` возвращает `true`

4. **Промокод не скрывается**
   - Проверьте `v-if="!isFreezeActive"` в `RightExchangerBlock.vue`
   - Убедитесь что composable инициализирован в компоненте

## 📊 Тестирование производительности

### Проверка анимаций на разных устройствах:

1. **Desktop** - все анимации должны работать плавно
2. **Mobile** - анимации адаптированы под меньший экран
3. **Слабые устройства** - при необходимости можно отключить сложные анимации

### Оптимизация при проблемах с производительностью:

В `FreezeModal.vue` и `FreezeBadge.vue` можно:
- Уменьшить количество частиц (snowflakes, wind particles)
- Упростить backdrop-filter эффекты
- Отключить анимации через CSS media queries

## 🔄 Обновление и откат

### Отключение акции:

Просто установите в `.env`:
```env
NUXT_GAMIFICATION_FREEZE_ENABLED=false
```

Перезапустите сервер:
```bash
npm run dev  # для разработки
pm2 restart mfz-exchanger  # для production
```

### Полное удаление:

Если нужно полностью удалить функциональность:

1. Удалите `GamificationFreezeWrapper` из `app.vue`
2. Удалите папку `composables/gamification/`
3. Удалите папку `components/Gamification/`
4. Откатите изменения в `useFactor.ts`
5. Откатите изменения в `RightExchangerBlock.vue`
6. Удалите переменные из `nuxt.config.ts` и `.env`

## 📝 Дополнительные возможности

### Кастомизация внешнего вида:

Все стили находятся в scoped секциях компонентов:
- `components/Gamification/FreezeModal.vue` - модалка
- `components/Gamification/FreezeBadge.vue` - плашка

Основные CSS переменные для кастомизации:
```scss
$freeze-blue: #00d4ff;
$freeze-white: #ffffff;
$freeze-dark: #0a2540;
```

### Интеграция с Telegram Mini App:

При наличии Telegram SDK можно добавить:
- Haptic feedback при активации
- Уведомления через Telegram
- Показ акции только для Telegram пользователей

## 📞 Поддержка

При возникновении проблем:
1. Проверьте консоль браузера на ошибки
2. Проверьте логи сервера
3. Убедитесь что все файлы созданы корректно
4. Проверьте версию Node.js и зависимостей
